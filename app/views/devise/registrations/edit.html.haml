.row
  .col-md-6 col-md-offset-3
%legend.text-center
  Thông tin tài khoản
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put}) do |f|
  = devise_error_messages!
  .col-md-12
    .form-group
      = f.label :email
      = f.email_field :email, autofocus: true, class: 'form-control'
  - if devise_mapping.confirmable? && resource.pending_reconfirmation?
    .col-md-12
      Currently waiting confirmation for: #{resource.unconfirmed_email}
  .col-md-12
    .form-group
      = f.label :password
      %i (leave blank if you don't want to change it)
      = f.password_field :password, autocomplete: "off", class: 'form-control'
  .col-md-12
    .form-group
      = f.label :password_confirmatio
      = f.password_field :password_confirmation, autocomplete: "off", class: 'form-control'
  .col-md-12
    .form-group
      = f.label :current_password
      %i (we need your current password to confirm your changes)
      = f.password_field :current_password, autocomplete: "off", class: 'form-control'
  .col-md-12= f.submit "Update", class: 'btn btn-primary pull-right'
%h3.text-center Xóa toàn khoản
%p.text-center
  Cảm thấy đéo vui thì xóa ! #{button_to "Delete", registration_path(resource_name), data: { confirm: "Are you sure ?" },class: 'btn btn-danger pull-right', method: :delete}
= link_to "Back", :back, class: 'btn btn-info pull-right', style: 'margin-right: 10px'